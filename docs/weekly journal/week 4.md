Week 4:  
Date: [15/08/2026]  
 
Progress Update: [During this week I went back and made a Successful Petitions dataset that is identical to the Master but only includes Successful datasets, I created 3 sets of linear regression models, one set for each Research Question, previously I used Successful as the variable for the Question 3 Models but I found that there was much better performance when I used the Successful dataset for those questions instead of having it as a variable. Question 1 Regression models rejected the null hypothesis with an explained variance of roughly 10% and a p value < 0.05 showing once more that the country level variables explain a limited portion of the variance in raw signature counts. The question 2 and question 3 models failed to reject the null hypothesis but there was notable improvement in the question 3 models, this maybe due to the fact that unsuccessful models appear to be more unpredictable and have a higher degree of variability while successful models are more predictable, as a result I revisited EDA to conduct some extra analyses on this and I found that it confirmed my suspicions. I then proceeded forward with Log Transformed Models for Question 2 and 3 using the same variables. The Question 2 models failed to reject the null hypothesis but there was significant improvement in the Quetion 3 models with one of them explaining 11% of the variance in Log Signatures per capita, this means the null hypothesis is rejected for question 3. I then revisted the PCA models and added a log linear regression counter part to each model, the 2nd log linear regression model provided one of the best R squared values so far with 13% explained variance supporting Question 3, however the first log model still failed to reject null hypothesis for question 2. I then created random forest regression models, with training and test splits in the data, each model provided really high r squared values in training phase but had really poor r squared values in the testing phase, this may be due to overfitting, extreme variability in the master dataset and lack of depth in the successful dataset, as a result of time constraints I wont be able to explore this avenue fully but I am satisfied with the results I have recieved through the Log Transformed models in the PCA and Log LInear Regression Stages. To touch things off I did a small cluster analysis that grouped countries into clusters based on the newly added socio-economic variables and then created a map and a pca plot, the map is eerily similar to the trends that I have seen while looking at the raw signature counts of each petition and it also strikes a resemblence to the map used in the Media Literacy Index 2023 paper. I then constructed a final Jupyter Notebook that aggregates the results of each exported model into one place for ease of use. ]  
Next Steps: [Go back through each Jupyter Notebook and revise and add Markdown comments and Evaluations where needed. Start work on the Thesis Paper.]  

Matthew Riddell